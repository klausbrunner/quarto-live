# Pyodide integration test

Requires a built runtime and internet access. From the repository root:

```sh
quarto preview tests
```

Check `pyodide.html`:

- Python version printed
- Successful grading, including after rerun
- Python and OJS output: `42`
- Line plot displayed
- R confirmation printed
